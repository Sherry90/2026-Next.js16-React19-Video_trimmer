const config = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {}, // Only needed if you want it, v4 handles prefixing mostly on its own but good for compat
  },
};

export default config;
