services:
  video-trimmer:
    build:
      context: .
      dockerfile: Dockerfile
    image: trimvideo-app:latest
    hostname: trimvideo.net
    ports:
      - "443:443"
    volumes:
      - ./certificates:/app/certificates:ro
    environment:
      HOSTNAME: "0.0.0.0"
      APP_URL: "https://trimvideo.net"
    restart: unless-stopped
